# CWE Navigation UI/UX 设计需求

## 1. 产品定位

CWE Navigation 是面向 CWE（Common Weakness Enumeration）Research Concepts View（View ID: 1000）的交互式知识图谱网站。

它不只是把官方关系图搬到网页上，而是让安全研究人员、开发者和学习者更容易完成以下任务：

- 快速找到一个 CWE。
- 判断它在 CWE 知识体系中的位置。
- 理解它与父级、子级及其他弱点的关系。
- 从一个弱点自然地继续探索相关弱点。
- 在复杂图谱中始终知道自己正在看什么、从哪里来。

网站保持轻量、无需登录。用户进入页面即可搜索和浏览。

## 2. 体验目标与原则

### 2.1 体验目标

- 将原先“左侧文本 + 右侧图表”的演示页面，升级为清晰、现代、适合持续探索的知识图谱工作台。
- 第一次访问的用户应在数秒内明白网站用途，并能立即通过搜索或图谱开始探索。
- 优先保证信息可读、操作可发现、上下文不丢失，不追求在一个画面中塞入尽可能多的内容。
- 在桌面端提供高效的研究体验，在手机端保留完整的查找、浏览和阅读能力。

### 2.2 设计原则

- **搜索优先**：知道目标的用户不必在大图中手动寻找。
- **先定位，再展开**：先让用户找到节点，再逐步展示关系和详细资料。
- **聚焦但不迷路**：强调当前节点，同时保留探索路径和整体位置感。
- **逐层披露**：画布解释结构，详情面板解释内容，官方文档承载完整资料。
- **操作可逆**：筛选、聚焦和节点跳转都有清晰的返回或清除方式。
- **视觉语义一致**：同一种颜色、形状和线型在全站表达同一种含义。
- **不只依赖颜色**：重要分类和状态同时通过文字、形状、图标或线型表达。

## 3. 整体页面结构

桌面端采用“顶部应用栏 + 左侧控制栏 + 中央图谱画布 + 右侧详情面板”的工作台布局。

- 顶部应用栏负责品牌、全局搜索和辅助入口。
- 左侧控制栏负责视图切换、图例和筛选。
- 中央画布始终是最大的视觉区域。
- 右侧详情只在选中节点后出现，未使用时不占空间。

页面各区域层级明确，但不应像传统后台一样被大量卡片和边框切碎。

## 4. 顶部应用栏与 CWE 搜索

### 4.1 顶部应用栏

- 左侧展示简洁的产品标识 `CWE Navigation`，旁边以低强调度显示当前 View 名称。
- 中间放置全局 CWE 搜索栏，它是顶栏中最醒目的交互控件。
- 右侧提供：
  - `About this data`：查看数据来源、View ID 和更新时间。
  - `View on GitHub`：前往项目页面。
  - 帮助入口：查看鼠标、触控和键盘操作说明。
- 顶栏紧凑且始终可见，让用户在探索到任何位置时都能立即发起新搜索。

### 4.2 搜索栏外观

- 搜索栏是一个完整的组合控件，由以下部分从左到右组成：
  - 小型搜索范围下拉框。
  - 细分隔线。
  - 搜索图标与主输入框。
  - 有输入内容时出现的清除按钮。
- 范围下拉框保持紧凑，宽度只需容纳当前选项，不应抢占主输入区域。
- 整个组合控件共享同一外轮廓和焦点状态，看起来像一个搜索栏，而不是两个拼接的表单控件。
- 默认占位提示为 `Search CWE ID, name, or description…`。
- 搜索栏获得焦点时使用清晰但克制的强调色描边，并在下方展开结果面板。

### 4.3 搜索范围

下拉框默认选择 `All fields`，并提供以下范围：

- `All fields`：在所有可搜索信息中查找。
- `CWE ID`：只按编号查找，例如 `79` 或 `CWE-79`。
- `Name`：只按弱点名称查找。
- `Description`：只搜索描述内容。

切换范围后，输入内容保持不变并立即刷新结果。当前范围在下拉框关闭时也必须清晰可见，避免用户忘记自己为何搜不到某项内容。

### 4.4 搜索结果面板

- 用户输入时，在搜索栏正下方显示即时结果面板，不跳离当前图谱。
- 每条结果展示：
  - CWE ID。
  - 弱点名称。
  - Abstraction 徽标。
  - 一行匹配摘要；搜索描述时应展示命中词附近的文本。
- 关键词在 ID、名称或摘要中高亮，但不能降低正文可读性。
- 完全匹配的 CWE ID 优先显示，其余结果按相关程度排列。
- 结果面板底部显示结果数量；结果较多时保持面板尺寸稳定并在内部滚动。
- 当前键盘选中的结果使用明显的背景和左侧强调条，与鼠标悬停状态保持一致。
- 无结果时显示 `No CWE found`，并提示用户尝试其他关键词或切换搜索范围。
- 输入为空时可显示简短搜索说明，不展示容易干扰用户的冗长内容。

### 4.5 搜索交互

- 用户可以用方向键浏览结果、按 `Enter` 打开、按 `Esc` 关闭结果面板。
- 点击结果后：
  - 结果面板关闭。
  - 图谱将目标节点带到清晰可见的位置。
  - 目标节点进入选中状态，直接关系同步高亮。
  - 右侧打开对应详情面板。
- 搜索词保留在输入框中，便于用户确认当前定位来源；点击清除按钮后恢复完整图谱视野。
- 搜索结果属于其他图谱视图时，应在结果中提前标明；选择后平顺进入对应视图，避免产生“节点突然消失”的感觉。

## 5. 左侧控制栏

- 左侧使用约 280px 的窄栏承载“控制”，而不是承载长篇节点详情。
- 控制栏可折叠；折叠后保留一个容易发现的展开按钮。
- 内容从上到下依次为：
  - 图谱视图选择器，显示易读的视图名称和节点数量。
  - Abstraction 图例与筛选器。
  - 关系类型说明与筛选器。
  - 当前已启用的筛选条件。
  - `Clear all` 操作。
- 每个图例项同时展示颜色/形状、名称和当前节点数量。
- 图例项可以直接开关，既解释图谱，也承担筛选功能。
- 筛选后应立即显示当前结果数量；没有节点时给出清晰说明和恢复全部节点的入口。

## 6. 中央图谱画布

### 6.1 画布布局

- 图谱画布占据页面最大的可用区域，背景安静、留白充足，不与节点争夺注意力。
- 左上角显示轻量探索路径，例如 `All nodes / CWE-707 / CWE-79`；点击路径项可返回之前查看的节点。
- 右下角提供一组紧凑的画布工具：
  - 放大。
  - 缩小。
  - 适应屏幕。
  - 回到初始视图。
  - 全屏/专注模式。
- 左下角显示缩放比例及一句简短操作提示。
- 第一次访问时可以出现一次轻量引导，指出搜索、拖动画布和选择节点三项核心操作；引导不得持续遮挡图谱。
- 图谱很大且容易迷失方向时可显示小地图；图谱较小时不显示。

### 6.2 节点与关系的视觉表达

- 节点标签优先显示 CWE ID 和短名称。
- 缩小视图时减少次要标签，只保留关键节点或 CWE ID；放大后逐步显示完整名称，避免文字堆叠。
- 节点颜色表达 Abstraction，形状或内部标记提供第二重分类提示。
- 节点大小仅在代表明确含义时变化，例如直接关系数量；图例必须解释该含义。
- 有方向的关系使用箭头，不同关系类型使用可辨别的线型。
- 连接线保持低强调度，只有在悬停、选中或聚焦时才提升对比度。

### 6.3 节点状态

- **默认状态**：节点和边清晰但克制，不让整个图谱同时争夺注意力。
- **悬停状态**：节点轮廓增强，并预高亮其直接关系；旁边出现紧凑提示卡。
- **选中状态**：节点显示高对比强调环，直接邻居和连接线保持清晰，其余内容降低透明度。
- **聚焦状态**：当前节点及一跳关系成为主要内容，无关节点进一步弱化，同时持续显示 `Show all nodes`。
- **筛选状态**：被排除的内容使用统一方式隐藏，不在同一页面混用“隐藏”和“半透明”两套含义。
- 状态变化使用短而克制的过渡，帮助用户理解变化来源，不制造漂移和等待感。

### 6.4 图谱操作

- 单击节点：选中节点并打开详情。
- 双击节点：进入聚焦模式，只突出该节点及其直接关系。
- 单击相邻节点：将其设为当前节点，并把原节点加入探索路径。
- 单击画布空白区域：清除节点选择并关闭详情，但保留当前缩放位置和筛选条件。
- 用户可以平移、缩放和拖拽节点，以调整当前观察角度。
- 悬停提示卡只展示 CWE ID、名称、Abstraction 和关系数量，完整描述留在详情面板。

## 7. 右侧节点详情面板

### 7.1 打开与关闭

- 未选中节点时，详情面板完全收起，画布获得完整空间。
- 画布边缘可显示低干扰提示：`Select a node to inspect its details`。
- 选中节点后，详情面板从右侧平顺展开；画布同步为它让出空间，目标节点不能被面板遮挡。
- 面板宽度约 380–440px，内部独立滚动。
- 关闭面板只清除当前节点选择，不重置筛选、缩放和探索路径。

### 7.2 信息层级

详情面板顶部保持以下内容可见：

- CWE ID，使用等宽字体和低强调度标签。
- 弱点名称，作为主标题。
- Abstraction 徽标。
- `Open official document` 主操作。
- `Copy link` 次操作，用于分享当前节点和浏览上下文。

正文按以下顺序展示：

- **Description**：优先展示，使用舒适的行宽和行距。
- **Vulnerability Mapping**：使用独立状态徽标或说明区块，不与普通描述混成一段。
- **Related Weaknesses**：以可点击列表显示 CWE ID、名称和关系类型。

点击 Related Weaknesses 后，用户留在站内继续探索，详情内容更新，原节点进入探索路径。没有相关弱点时不显示空区块。

## 8. 连续探索与上下文

- 当前图谱视图、搜索来源、选中节点、筛选条件和聚焦状态都应在界面中有可见反馈。
- 从搜索结果或相关弱点进入节点后，探索路径记录用户经过的节点。
- 用户可以通过路径返回任意上一步，而不需要重新搜索。
- `Copy link` 生成的链接再次打开时，应回到相同图谱并重新选中同一节点。
- 切换图谱视图时，如果当前节点不在新视图中，应提前说明它将不再显示，而不是直接清空界面。

## 9. 响应式体验

### 9.1 桌面与平板横屏

- 保持完整工作台布局。
- 宽度不足时先折叠左侧控制栏，再适当缩窄详情面板，中央画布始终保有可操作空间。
- 顶部搜索栏保持居中和醒目，不因两侧入口增多而被挤压。

### 9.2 手机与平板竖屏

- 顶栏首行显示产品名和必要操作，搜索栏独占下一行。
- 点击搜索栏后进入专注搜索状态，结果面板占据主要屏幕；范围下拉框始终保留在输入框左侧。
- 图谱占据主屏，筛选器使用全屏抽屉，节点详情使用可拖动的底部面板。
- 底部详情面板支持半屏和接近全屏两档：半屏时兼顾关系观察，全屏时方便阅读长描述。
- 触控目标不小于 44×44px，支持双指缩放和平移。
- 不把悬停作为获取信息的唯一方式，也不强行同时展示图例、详情和图谱。

## 10. 视觉风格

- 整体气质接近现代研究工具，而不是传统后台管理系统或炫技式数据大屏。
- 使用偏冷的中性色背景、白色或轻微着色的面板和一个高辨识度强调色。
- 分类色采用稳定、色盲友好的色板，在图谱、图例和详情徽标中保持一致。
- 通过细边框、背景层级和少量柔和阴影区分区域，避免厚重阴影和层层卡片嵌套。
- 标题清晰并带适度技术感；正文使用高可读性字体；数字和 CWE ID 使用等宽字体，方便快速扫描。
- 圆角、间距、控件高度和图标尺寸采用统一尺度。
- 页面文案使用简洁英文，并与 CWE 官方术语保持一致。
- 若提供深色主题，两套主题都必须保证节点、连接线、搜索结果和正文有足够对比度；否则优先完成高质量浅色主题。

## 11. 状态反馈与可访问性

### 11.1 页面状态

- 初次进入时显示与最终页面结构一致的轻量加载状态，避免空白屏和明显跳动。
- 图谱暂不可用时，中央区域显示简洁说明与 `Retry` 操作。
- 节点资料不可用时，仍显示可确认的 CWE ID 和名称，并明确说明详细资料暂不可用。
- “页面无数据”“搜索无结果”“筛选无结果”和“详情缺失”使用不同文案和恢复操作。
- 操作成功不滥用弹窗；复制链接等轻量反馈使用短暂提示。

### 11.2 键盘与辅助体验

- 搜索栏、范围下拉框、结果列表、筛选器、面板关闭和画布工具都可通过键盘操作。
- 所有可操作元素都有清晰的悬停、按下和键盘焦点状态。
- `Esc` 优先关闭当前最上层内容，例如范围菜单、搜索结果、聚焦模式或详情面板。
- 提供“跳到图谱”和“跳到节点详情”的快速跳转入口。
- 为当前选中节点及其直接关系提供结构化文本列表，使不便操作图谱的用户也能继续浏览。
- 动效尊重用户的减少动态效果偏好；文字、控件和关键图形状态达到清晰可辨的对比度。

## 12. UI/UX 验收场景

新版体验至少应自然完成以下场景：

- 用户在顶部选择 `CWE ID`，输入 `79`，从结果中打开 CWE-79。
- 用户选择 `Description`，输入一个概念词，通过命中摘要判断哪个结果更相关。
- 用户在搜索范围之间切换时，输入内容保持不变，结果立即反映新的范围。
- 用户从搜索结果进入节点，查看其直接关系和详情，并继续进入两个 Related Weaknesses。
- 用户通过探索路径返回之前查看的节点。
- 用户按 Abstraction 筛选图谱，并一键清除筛选。
- 用户缩放、平移、拖拽节点，再一键让图谱适应屏幕。
- 用户复制当前节点链接，重新打开后回到相同节点。
- 用户在无搜索结果、无筛选结果或资料缺失时，能理解发生了什么并知道下一步怎么做。
- 用户仅使用键盘完成范围选择、搜索、结果选择、详情阅读和关闭面板。
- 用户在手机上完成范围搜索、查看图谱、打开底部详情并继续探索相关节点。

{
  "name": "cwe-data-scripts-nodejs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "npm workspace: CWE data pipeline that generates public/ data for the frontend",
  "scripts": {
    "generate": "tsx src/generate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "gen:fixture": "tsx ../fixture/update-baseline.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "fast-xml-parser": "^5.2.0",
    "fflate": "^0.8.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.1.0"
  }
}
